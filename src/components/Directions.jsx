export const Directions = () => {
  return null
  //   create the arrows with directions from AudioParam, connect them to the API call function and once clicked increase the progress and render the new descriptions and direction
}
